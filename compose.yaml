services:
  db:
    image: postgres:18
    container_name: dts-postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: dts
    ports:
      - "5432:5432"
    volumes:
      - dts_postgres_data:/var/lib/postgresql

volumes:
  dts_postgres_data: